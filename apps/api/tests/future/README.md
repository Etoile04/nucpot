# Future Tests (Phase 2)

These tests were written for endpoints not yet implemented in Phase 1 MVP.
They are preserved here for Phase 2 development.

## Files

- `test_materials_api.py` — Integration tests for `/api/v1/materials` endpoints
- `test_materials_router.py` — Tests for materials REST router
- `test_properties_router.py` — Tests for properties REST router
- `test_sources_router.py` — Tests for sources REST router

## When to re-enable

Move these files back to `tests/` once the corresponding routers and endpoints
are implemented (Phase 2 roadmap: NFM-674).
