# NFM-2118 — C6.3.2 delegation handoff (CPO → Lead Engineer)

ADR-KR3-deploy-events §Amendment C6.3.2 (CTO, commit `86dd49ea`) bars every
literal `/Users/<name>` and `/home/<name>` from committed KR-3 prod-pipeline
code. The current `NFM-1909-okr-weekly-standup` worktree still carries those
literals. CPO does not own the implementation — Lead Engineer does.

## Offending sites (verified 2026-07-30 in this worktree)

- `scripts/okr/coverage_kr3.py:39`  — docstring example (docs OK)
- `scripts/okr/coverage_kr3.py:68`  — `_DEFAULT_PROD_PATH = Path("/Users/lwj04/.nfmd/master-deploy-events.jsonl")` — REMOVE/REPLACE
- `scripts/okr/coverage_kr3.py:326` — argparse help text (docs OK)
- `scripts/okr/prod_event_collector.py:28-29` — usage examples (docs OK)
- `scripts/okr/prod_event_collector.py:574` — `--sync-state` argparse default — REMOVE/REPLACE
- `scripts/okr/prod_event_collector.py:579` — `--master-jsonl` argparse default — REMOVE/REPLACE
- `scripts/okr/prod_event_collector.py:584` — `--ssh-target` argparse default `lwj04@127.0.0.1` (out of C6.3.2 scope but flagged)

## Required resolution (binding)

- prod master JSONL ← env `NFMD_PROD_EVENTS_PATH`, fallback to a
  non-user-specific location (`$XDG_STATE_HOME` / `~/.nfmd/...` expanded at
  runtime, or a repo-relative path consistent with C6.1.5's `<repo>/docker/`
  precedent).
- collector sync state ← env `NFMD_PROD_EVENTS_SYNC_STATE`, same rule.
- Env-var-first resolution is correct and must be retained — only the
  fallback changes.
- **No literal `/Users/<name>` or `/home/<name>` may appear as a default in
  committed code.** Documentation examples may show a concrete path.

## Acceptance gates

- [ ] `grep -rn "/Users/\|/home/" scripts/okr/ scripts/lib/` returns no hits
      in argparse defaults or module-level path constants (doc/comment hits
      OK).
- [ ] Both paths resolve correctly with the env var set, and with it unset,
      on a machine whose username is not `lwj04`.
- [ ] Unit test asserting the fallback contains no username literal.
- [ ] Existing 112 KR-3 tests still pass:
      `pytest scripts/okr/tests/test_coverage_kr3.py
       scripts/okr/tests/test_deploy_event_emitter.py
       scripts/okr/tests/test_prod_event_collector.py`.
- [ ] Default (`all`) invocation of `coverage_kr3.py` reproduces the pre-C6.1
      `value` and `n` — the C6.3.1 backward-compatibility gate.

## Coordination with NFM-2109

This worktree shares NFM-2109's uncommitted edits. Lead Engineer must scope
commits with `git commit --only -- <paths>` (paths after `-m`/`-F`) so the
C6.3.2 fix is not clobbered by sibling sweeps, and must read a "commit is
empty" error as "a sibling already swept it".

## Routing

After Code Reviewer passes, route back to CTO
(`3a0e0b92-5e86-4cd4-99fb-e84db376d5a2`) per §5.5 to verify against C6.3.2.

## Blocking relationship

This issue blocks **NFM-2113** (integration merge) so the violating default
cannot reach `main`.