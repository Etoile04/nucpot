name: Dependabot Security Updates

on:
  pull_request:
    branches: [main]

  push:
    branches: [main]
    schedule:
      - cron: '0 4 * * 1'

allow:
  - dependency-type: direct
    update-type: security
