# NFM-3582: STANDARD_PROPERTIES ontology coverage

Generated by `apps/api/scripts/ontology_property_coverage_report.py`. The pytest gate in `tests/test_ontology_property_coverage.py` uses the same classifier and asserts every alias resolves through `property_categories[].standard_properties` ∪ `entity_types[].required_properties`.

## Sources

- Alias dictionary: `apps.api.src.nfm_db.core.property_catalog.STANDARD_PROPERTIES` (113 alias(es) → 74 unique standard name(s))
- Canonical ontology fixture: `/Users/lwj04/.paperclip/instances/default/workspaces/98fc3168-be45-4673-808e-22238b366352/nucpot/.worktrees/NFM-3582-ontology-coverage-report/apps/api/tests/fixtures/canonical_ontology.json`
- Ontology loader contract: `apps.api.src.nfm_db.services.extraction_prompt._build_ontology_standard_names_block`

## Headline metrics

- Total aliases: **113**
- Unique standard names: **74**
- Mapped (alias-level): **0** (0.0%)
- Partial: **0** (name in only one ontology surface)
- Missing: **113** (name absent from the ontology)
- Standard-name coverage (de-duplicated): **0 / 74** (0.0%)

Coverage gate: **FAIL** ❌

Follow-up issues MUST be filed for every row below with status ≠ mapped; the AC for NFM-3582 is satisfied once the gate passes (i.e., every alias has status `mapped`).

## Per-alias coverage

| Alias | Standard name | Status | Remediation |
| --- | --- | --- | --- |
| `annealing temperature` | `热处理温度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '热处理温度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `average cte` | `平均线膨胀系数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '平均线膨胀系数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `average void size` | `平均空洞尺寸` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '平均空洞尺寸' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `bubble density` | `气泡密度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '气泡密度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `bubble number density` | `气泡密度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '气泡密度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `bulk modulus` | `体积模量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '体积模量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `burst strain` | `断裂应变` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '断裂应变' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `cold reduction` | `冷加工量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '冷加工量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `cold work` | `冷加工量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '冷加工量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `constitutive parameter` | `本构模型参数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '本构模型参数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `corrosion fatigue crack growth rate` | `腐蚀疲劳裂纹扩展速率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '腐蚀疲劳裂纹扩展速率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `corrosion rate` | `腐蚀速率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '腐蚀速率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `cp` | `定压比热容` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '定压比热容' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `crack growth rate` | `裂纹扩展速率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '裂纹扩展速率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `creep compliance` | `蠕变柔量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '蠕变柔量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `creep exponent` | `应力指数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '应力指数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `creep law parameter` | `Norton蠕变参数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals 'Norton蠕变参数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `creep strain` | `蠕变应变` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '蠕变应变' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `creepdown strain` | `蠕变应变` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '蠕变应变' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `d content` | `氘含量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '氘含量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `d-spacing` | `晶面间距` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '晶面间距' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `density` | `密度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '密度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `deuterium content` | `氘含量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '氘含量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `deuterium pickup` | `吸氘率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '吸氘率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `deuterium uptake` | `吸氘率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '吸氘率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `dimensional change` | `尺寸变化率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '尺寸变化率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `dislocation density` | `位错密度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '位错密度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `dislocation hardening parameter` | `位错硬化参数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '位错硬化参数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `ductility` | `延伸率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '延伸率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `elastic constants` | `弹性常数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '弹性常数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `elastic modulus` | `杨氏模量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '杨氏模量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `elongation` | `延伸率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '延伸率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `extrusion temperature` | `挤压温度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '挤压温度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `flow stress` | `流动应力` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '流动应力' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `fracture strain` | `断裂应变` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '断裂应变' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `fuel segment length` | `燃料段长度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '燃料段长度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `fuelled length` | `燃料段长度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '燃料段长度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `gas bubble swelling` | `气泡肿胀率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '气泡肿胀率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `grain size` | `晶粒尺寸` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '晶粒尺寸' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `h content` | `氢含量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '氢含量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `h pickup fraction` | `吸氢率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '吸氢率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `hardening modulus` | `硬化模量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '硬化模量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `hardness` | `硬度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '硬度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `heat capacity` | `比热容` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '比热容' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `heat capacity at constant pressure` | `定压比热容` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '定压比热容' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `heat treatment temperature` | `热处理温度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '热处理温度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `hydrogen content` | `氢含量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '氢含量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `hydrogen pickup` | `吸氢率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '吸氢率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `increase in yield stress` | `强度增量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '强度增量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `inside diameter` | `样品内径` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '样品内径' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `instantaneous cte` | `瞬时线膨胀系数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '瞬时线膨胀系数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `instantaneous thermal expansion` | `瞬时线膨胀系数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '瞬时线膨胀系数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `interplanar distance` | `晶面间距` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '晶面间距' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `irradiation creep rate` | `辐照蠕变速率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '辐照蠕变速率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `irradiation creep strain` | `蠕变应变` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '蠕变应变' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `irradiation growth` | `尺寸变化率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '尺寸变化率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `irradiation hardening` | `辐照硬化量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '辐照硬化量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `johnson-cook` | `Johnson-Cook参数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals 'Johnson-Cook参数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `kearns factor` | `织构系数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '织构系数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `lattice parameter a` | `晶格参数a` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '晶格参数a' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `lattice parameter c` | `晶格参数c` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '晶格参数c' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `mass gain` | `氧化增重` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '氧化增重' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `material constant` | `本构模型参数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '本构模型参数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `mean thermal expansion` | `平均线膨胀系数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '平均线膨胀系数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `mean void diameter` | `平均空洞尺寸` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '平均空洞尺寸' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `microhardness` | `硬度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '硬度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `norton creep` | `Norton蠕变参数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals 'Norton蠕变参数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `outside diameter` | `样品外径` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '样品外径' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `oxidation rate` | `氧化速率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '氧化速率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `oxide layer thickness` | `氧化膜厚度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '氧化膜厚度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `oxide thickness` | `氧化膜厚度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '氧化膜厚度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `phase fraction` | `相分数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '相分数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `pit density` | `点蚀密度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '点蚀密度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `pitting density` | `点蚀密度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '点蚀密度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `poisson's ratio` | `泊松比` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '泊松比' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `precipitate size` | `析出相尺寸` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '析出相尺寸' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `ramberg-osgood` | `Ramberg-Osgood参数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals 'Ramberg-Osgood参数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `relative density` | `相对密度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '相对密度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `rupture time` | `失效时间` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '失效时间' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `scc threshold` | `应力腐蚀阈值` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '应力腐蚀阈值' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `shear modulus` | `剪切模量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '剪切模量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `specific heat` | `比热容` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '比热容' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `spp size` | `析出相尺寸` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '析出相尺寸' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `steady-state creep rate` | `稳态蠕变速率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '稳态蠕变速率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `stiffness tensor` | `弹性常数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '弹性常数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `strain hardening exponent` | `硬化指数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '硬化指数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `strength increment` | `强度增量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '强度增量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `stress corrosion cracking threshold` | `应力腐蚀阈值` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '应力腐蚀阈值' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `stress exponent` | `应力指数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '应力指数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `tensile strength` | `抗拉强度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '抗拉强度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `texture coefficient` | `织构系数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '织构系数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `theoretical density` | `理论密度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '理论密度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `thermal conductivity` | `热导率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '热导率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `thermal diffusivity` | `热扩散率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '热扩散率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `thermal expansion strain` | `热膨胀应变` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '热膨胀应变' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `thermal resistance` | `热阻` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '热阻' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `time-to-failure` | `失效时间` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '失效时间' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `transient creep rate` | `瞬态蠕变速率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '瞬态蠕变速率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `ultimate strength` | `抗拉强度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '抗拉强度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `uts` | `抗拉强度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '抗拉强度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `vickers hardness` | `硬度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '硬度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `void density` | `空洞密度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '空洞密度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `void swelling` | `空洞肿胀率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '空洞肿胀率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `volume fraction` | `相分数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '相分数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `volume swelling` | `体积膨胀率` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '体积膨胀率' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `volumetric thermal expansion` | `体膨胀系数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '体膨胀系数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `wall thickness` | `壁厚` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '壁厚' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `weight gain` | `氧化增重` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '氧化增重' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `yield criterion` | `屈服准则参数` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '屈服准则参数' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `yield strength` | `屈服强度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '屈服强度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `yield strength increase` | `辐照硬化量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '辐照硬化量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `yield stress` | `屈服强度` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '屈服强度' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |
| `young's modulus` | `杨氏模量` | ❌ missing (in: <none>) | Add a new ontology entry whose standard name equals '杨氏模量' — either a row under property_categories or under an entity_type's required_properties — so the alias resolves through the ontology loader. |

## Missing — fill canonical ontology fixture

These standard names do not appear in either ontology surface. Add a new ontology entry whose `standard_properties` (under `property_categories[]`) or `required_properties` (under `entity_types[]`) equals the listed name.

| Standard name | Aliases (legacy) | Suggested ontology node |
| --- | --- | --- |
| `Johnson-Cook参数` | `johnson-cook` | Add a `property_categories[]` entry whose `standard_properties` includes `Johnson-Cook参数`, OR add `Johnson-Cook参数` to an `entity_types[].required_properties[]` list. |
| `Norton蠕变参数` | `creep law parameter`, `norton creep` | Add a `property_categories[]` entry whose `standard_properties` includes `Norton蠕变参数`, OR add `Norton蠕变参数` to an `entity_types[].required_properties[]` list. |
| `Ramberg-Osgood参数` | `ramberg-osgood` | Add a `property_categories[]` entry whose `standard_properties` includes `Ramberg-Osgood参数`, OR add `Ramberg-Osgood参数` to an `entity_types[].required_properties[]` list. |
| `位错密度` | `dislocation density` | Add a `property_categories[]` entry whose `standard_properties` includes `位错密度`, OR add `位错密度` to an `entity_types[].required_properties[]` list. |
| `位错硬化参数` | `dislocation hardening parameter` | Add a `property_categories[]` entry whose `standard_properties` includes `位错硬化参数`, OR add `位错硬化参数` to an `entity_types[].required_properties[]` list. |
| `体积模量` | `bulk modulus` | Add a `property_categories[]` entry whose `standard_properties` includes `体积模量`, OR add `体积模量` to an `entity_types[].required_properties[]` list. |
| `体积膨胀率` | `volume swelling` | Add a `property_categories[]` entry whose `standard_properties` includes `体积膨胀率`, OR add `体积膨胀率` to an `entity_types[].required_properties[]` list. |
| `体膨胀系数` | `volumetric thermal expansion` | Add a `property_categories[]` entry whose `standard_properties` includes `体膨胀系数`, OR add `体膨胀系数` to an `entity_types[].required_properties[]` list. |
| `冷加工量` | `cold reduction`, `cold work` | Add a `property_categories[]` entry whose `standard_properties` includes `冷加工量`, OR add `冷加工量` to an `entity_types[].required_properties[]` list. |
| `剪切模量` | `shear modulus` | Add a `property_categories[]` entry whose `standard_properties` includes `剪切模量`, OR add `剪切模量` to an `entity_types[].required_properties[]` list. |
| `吸氘率` | `deuterium pickup`, `deuterium uptake` | Add a `property_categories[]` entry whose `standard_properties` includes `吸氘率`, OR add `吸氘率` to an `entity_types[].required_properties[]` list. |
| `吸氢率` | `h pickup fraction`, `hydrogen pickup` | Add a `property_categories[]` entry whose `standard_properties` includes `吸氢率`, OR add `吸氢率` to an `entity_types[].required_properties[]` list. |
| `壁厚` | `wall thickness` | Add a `property_categories[]` entry whose `standard_properties` includes `壁厚`, OR add `壁厚` to an `entity_types[].required_properties[]` list. |
| `失效时间` | `rupture time`, `time-to-failure` | Add a `property_categories[]` entry whose `standard_properties` includes `失效时间`, OR add `失效时间` to an `entity_types[].required_properties[]` list. |
| `定压比热容` | `cp`, `heat capacity at constant pressure` | Add a `property_categories[]` entry whose `standard_properties` includes `定压比热容`, OR add `定压比热容` to an `entity_types[].required_properties[]` list. |
| `密度` | `density` | Add a `property_categories[]` entry whose `standard_properties` includes `密度`, OR add `密度` to an `entity_types[].required_properties[]` list. |
| `尺寸变化率` | `dimensional change`, `irradiation growth` | Add a `property_categories[]` entry whose `standard_properties` includes `尺寸变化率`, OR add `尺寸变化率` to an `entity_types[].required_properties[]` list. |
| `屈服准则参数` | `yield criterion` | Add a `property_categories[]` entry whose `standard_properties` includes `屈服准则参数`, OR add `屈服准则参数` to an `entity_types[].required_properties[]` list. |
| `屈服强度` | `yield strength`, `yield stress` | Add a `property_categories[]` entry whose `standard_properties` includes `屈服强度`, OR add `屈服强度` to an `entity_types[].required_properties[]` list. |
| `平均空洞尺寸` | `average void size`, `mean void diameter` | Add a `property_categories[]` entry whose `standard_properties` includes `平均空洞尺寸`, OR add `平均空洞尺寸` to an `entity_types[].required_properties[]` list. |
| `平均线膨胀系数` | `average cte`, `mean thermal expansion` | Add a `property_categories[]` entry whose `standard_properties` includes `平均线膨胀系数`, OR add `平均线膨胀系数` to an `entity_types[].required_properties[]` list. |
| `应力指数` | `creep exponent`, `stress exponent` | Add a `property_categories[]` entry whose `standard_properties` includes `应力指数`, OR add `应力指数` to an `entity_types[].required_properties[]` list. |
| `应力腐蚀阈值` | `scc threshold`, `stress corrosion cracking threshold` | Add a `property_categories[]` entry whose `standard_properties` includes `应力腐蚀阈值`, OR add `应力腐蚀阈值` to an `entity_types[].required_properties[]` list. |
| `延伸率` | `ductility`, `elongation` | Add a `property_categories[]` entry whose `standard_properties` includes `延伸率`, OR add `延伸率` to an `entity_types[].required_properties[]` list. |
| `弹性常数` | `elastic constants`, `stiffness tensor` | Add a `property_categories[]` entry whose `standard_properties` includes `弹性常数`, OR add `弹性常数` to an `entity_types[].required_properties[]` list. |
| `强度增量` | `increase in yield stress`, `strength increment` | Add a `property_categories[]` entry whose `standard_properties` includes `强度增量`, OR add `强度增量` to an `entity_types[].required_properties[]` list. |
| `抗拉强度` | `tensile strength`, `ultimate strength`, `uts` | Add a `property_categories[]` entry whose `standard_properties` includes `抗拉强度`, OR add `抗拉强度` to an `entity_types[].required_properties[]` list. |
| `挤压温度` | `extrusion temperature` | Add a `property_categories[]` entry whose `standard_properties` includes `挤压温度`, OR add `挤压温度` to an `entity_types[].required_properties[]` list. |
| `断裂应变` | `burst strain`, `fracture strain` | Add a `property_categories[]` entry whose `standard_properties` includes `断裂应变`, OR add `断裂应变` to an `entity_types[].required_properties[]` list. |
| `晶格参数a` | `lattice parameter a` | Add a `property_categories[]` entry whose `standard_properties` includes `晶格参数a`, OR add `晶格参数a` to an `entity_types[].required_properties[]` list. |
| `晶格参数c` | `lattice parameter c` | Add a `property_categories[]` entry whose `standard_properties` includes `晶格参数c`, OR add `晶格参数c` to an `entity_types[].required_properties[]` list. |
| `晶粒尺寸` | `grain size` | Add a `property_categories[]` entry whose `standard_properties` includes `晶粒尺寸`, OR add `晶粒尺寸` to an `entity_types[].required_properties[]` list. |
| `晶面间距` | `d-spacing`, `interplanar distance` | Add a `property_categories[]` entry whose `standard_properties` includes `晶面间距`, OR add `晶面间距` to an `entity_types[].required_properties[]` list. |
| `本构模型参数` | `constitutive parameter`, `material constant` | Add a `property_categories[]` entry whose `standard_properties` includes `本构模型参数`, OR add `本构模型参数` to an `entity_types[].required_properties[]` list. |
| `杨氏模量` | `elastic modulus`, `young's modulus` | Add a `property_categories[]` entry whose `standard_properties` includes `杨氏模量`, OR add `杨氏模量` to an `entity_types[].required_properties[]` list. |
| `析出相尺寸` | `precipitate size`, `spp size` | Add a `property_categories[]` entry whose `standard_properties` includes `析出相尺寸`, OR add `析出相尺寸` to an `entity_types[].required_properties[]` list. |
| `样品内径` | `inside diameter` | Add a `property_categories[]` entry whose `standard_properties` includes `样品内径`, OR add `样品内径` to an `entity_types[].required_properties[]` list. |
| `样品外径` | `outside diameter` | Add a `property_categories[]` entry whose `standard_properties` includes `样品外径`, OR add `样品外径` to an `entity_types[].required_properties[]` list. |
| `比热容` | `heat capacity`, `specific heat` | Add a `property_categories[]` entry whose `standard_properties` includes `比热容`, OR add `比热容` to an `entity_types[].required_properties[]` list. |
| `气泡密度` | `bubble density`, `bubble number density` | Add a `property_categories[]` entry whose `standard_properties` includes `气泡密度`, OR add `气泡密度` to an `entity_types[].required_properties[]` list. |
| `气泡肿胀率` | `gas bubble swelling` | Add a `property_categories[]` entry whose `standard_properties` includes `气泡肿胀率`, OR add `气泡肿胀率` to an `entity_types[].required_properties[]` list. |
| `氘含量` | `d content`, `deuterium content` | Add a `property_categories[]` entry whose `standard_properties` includes `氘含量`, OR add `氘含量` to an `entity_types[].required_properties[]` list. |
| `氢含量` | `h content`, `hydrogen content` | Add a `property_categories[]` entry whose `standard_properties` includes `氢含量`, OR add `氢含量` to an `entity_types[].required_properties[]` list. |
| `氧化增重` | `mass gain`, `weight gain` | Add a `property_categories[]` entry whose `standard_properties` includes `氧化增重`, OR add `氧化增重` to an `entity_types[].required_properties[]` list. |
| `氧化膜厚度` | `oxide layer thickness`, `oxide thickness` | Add a `property_categories[]` entry whose `standard_properties` includes `氧化膜厚度`, OR add `氧化膜厚度` to an `entity_types[].required_properties[]` list. |
| `氧化速率` | `oxidation rate` | Add a `property_categories[]` entry whose `standard_properties` includes `氧化速率`, OR add `氧化速率` to an `entity_types[].required_properties[]` list. |
| `泊松比` | `poisson's ratio` | Add a `property_categories[]` entry whose `standard_properties` includes `泊松比`, OR add `泊松比` to an `entity_types[].required_properties[]` list. |
| `流动应力` | `flow stress` | Add a `property_categories[]` entry whose `standard_properties` includes `流动应力`, OR add `流动应力` to an `entity_types[].required_properties[]` list. |
| `点蚀密度` | `pit density`, `pitting density` | Add a `property_categories[]` entry whose `standard_properties` includes `点蚀密度`, OR add `点蚀密度` to an `entity_types[].required_properties[]` list. |
| `热处理温度` | `annealing temperature`, `heat treatment temperature` | Add a `property_categories[]` entry whose `standard_properties` includes `热处理温度`, OR add `热处理温度` to an `entity_types[].required_properties[]` list. |
| `热导率` | `thermal conductivity` | Add a `property_categories[]` entry whose `standard_properties` includes `热导率`, OR add `热导率` to an `entity_types[].required_properties[]` list. |
| `热扩散率` | `thermal diffusivity` | Add a `property_categories[]` entry whose `standard_properties` includes `热扩散率`, OR add `热扩散率` to an `entity_types[].required_properties[]` list. |
| `热膨胀应变` | `thermal expansion strain` | Add a `property_categories[]` entry whose `standard_properties` includes `热膨胀应变`, OR add `热膨胀应变` to an `entity_types[].required_properties[]` list. |
| `热阻` | `thermal resistance` | Add a `property_categories[]` entry whose `standard_properties` includes `热阻`, OR add `热阻` to an `entity_types[].required_properties[]` list. |
| `燃料段长度` | `fuel segment length`, `fuelled length` | Add a `property_categories[]` entry whose `standard_properties` includes `燃料段长度`, OR add `燃料段长度` to an `entity_types[].required_properties[]` list. |
| `理论密度` | `theoretical density` | Add a `property_categories[]` entry whose `standard_properties` includes `理论密度`, OR add `理论密度` to an `entity_types[].required_properties[]` list. |
| `相分数` | `phase fraction`, `volume fraction` | Add a `property_categories[]` entry whose `standard_properties` includes `相分数`, OR add `相分数` to an `entity_types[].required_properties[]` list. |
| `相对密度` | `relative density` | Add a `property_categories[]` entry whose `standard_properties` includes `相对密度`, OR add `相对密度` to an `entity_types[].required_properties[]` list. |
| `瞬态蠕变速率` | `transient creep rate` | Add a `property_categories[]` entry whose `standard_properties` includes `瞬态蠕变速率`, OR add `瞬态蠕变速率` to an `entity_types[].required_properties[]` list. |
| `瞬时线膨胀系数` | `instantaneous cte`, `instantaneous thermal expansion` | Add a `property_categories[]` entry whose `standard_properties` includes `瞬时线膨胀系数`, OR add `瞬时线膨胀系数` to an `entity_types[].required_properties[]` list. |
| `硬化指数` | `strain hardening exponent` | Add a `property_categories[]` entry whose `standard_properties` includes `硬化指数`, OR add `硬化指数` to an `entity_types[].required_properties[]` list. |
| `硬化模量` | `hardening modulus` | Add a `property_categories[]` entry whose `standard_properties` includes `硬化模量`, OR add `硬化模量` to an `entity_types[].required_properties[]` list. |
| `硬度` | `hardness`, `microhardness`, `vickers hardness` | Add a `property_categories[]` entry whose `standard_properties` includes `硬度`, OR add `硬度` to an `entity_types[].required_properties[]` list. |
| `稳态蠕变速率` | `steady-state creep rate` | Add a `property_categories[]` entry whose `standard_properties` includes `稳态蠕变速率`, OR add `稳态蠕变速率` to an `entity_types[].required_properties[]` list. |
| `空洞密度` | `void density` | Add a `property_categories[]` entry whose `standard_properties` includes `空洞密度`, OR add `空洞密度` to an `entity_types[].required_properties[]` list. |
| `空洞肿胀率` | `void swelling` | Add a `property_categories[]` entry whose `standard_properties` includes `空洞肿胀率`, OR add `空洞肿胀率` to an `entity_types[].required_properties[]` list. |
| `织构系数` | `kearns factor`, `texture coefficient` | Add a `property_categories[]` entry whose `standard_properties` includes `织构系数`, OR add `织构系数` to an `entity_types[].required_properties[]` list. |
| `腐蚀疲劳裂纹扩展速率` | `corrosion fatigue crack growth rate` | Add a `property_categories[]` entry whose `standard_properties` includes `腐蚀疲劳裂纹扩展速率`, OR add `腐蚀疲劳裂纹扩展速率` to an `entity_types[].required_properties[]` list. |
| `腐蚀速率` | `corrosion rate` | Add a `property_categories[]` entry whose `standard_properties` includes `腐蚀速率`, OR add `腐蚀速率` to an `entity_types[].required_properties[]` list. |
| `蠕变应变` | `creep strain`, `creepdown strain`, `irradiation creep strain` | Add a `property_categories[]` entry whose `standard_properties` includes `蠕变应变`, OR add `蠕变应变` to an `entity_types[].required_properties[]` list. |
| `蠕变柔量` | `creep compliance` | Add a `property_categories[]` entry whose `standard_properties` includes `蠕变柔量`, OR add `蠕变柔量` to an `entity_types[].required_properties[]` list. |
| `裂纹扩展速率` | `crack growth rate` | Add a `property_categories[]` entry whose `standard_properties` includes `裂纹扩展速率`, OR add `裂纹扩展速率` to an `entity_types[].required_properties[]` list. |
| `辐照硬化量` | `irradiation hardening`, `yield strength increase` | Add a `property_categories[]` entry whose `standard_properties` includes `辐照硬化量`, OR add `辐照硬化量` to an `entity_types[].required_properties[]` list. |
| `辐照蠕变速率` | `irradiation creep rate` | Add a `property_categories[]` entry whose `standard_properties` includes `辐照蠕变速率`, OR add `辐照蠕变速率` to an `entity_types[].required_properties[]` list. |

## Reproducing this report

```bash
cd apps/api
python -m scripts.ontology_property_coverage_report \
    --output ../../docs/verification/NFM-3582-coverage.md

# Companion pytest gate (FAIL while fixture is empty)
python -m pytest tests/test_ontology_property_coverage.py -v
```
